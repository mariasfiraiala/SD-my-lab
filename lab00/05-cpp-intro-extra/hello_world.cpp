// The standard C++ input/output library is <iostream>.
#include <iostream>

int main()
{
    int x;

    // reading from STDIN with operator >>
    std::cin >> x;

    // std::cout: writing to STDOUT with operator <<
    std::cout << "Hello, Gigel! I got " << x << " from the SD team!";
    return 0;
}
